# Installers

No second installer is implemented. Foundation selects the pure Node package
under `distribution/package/`; it requires an existing supported Node and local
Docker installation. Add content here only if another installation mechanism
is explicitly selected without creating a competing Runtime or product
contract.
